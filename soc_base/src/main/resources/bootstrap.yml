spring:
  cloud:
    config:
      name: base
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: soc-config
eureka:
  client:
    service-url:
      defaultZone: http://172.17.0.1:6868/eureka/