spring:
  cloud:
    config:
      name: manager
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: soc-config
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:6868/eureka/